/* Global iOS fixes for both standalone and non-standalone mode */
@supports (-webkit-touch-callout: none) {
  /* Fix for global container to eliminate any gap at the bottom */
  html, body {
    /* Use full height with no overflow at body level */
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    bottom: 0 !important;
    width: 100% !important;
    height: 100% !important;
    margin: 0 !important;
    padding: 0 !important;
    overflow: hidden !important;
    /* Eliminate bounce effects */
    overscroll-behavior: none !important;
    -webkit-overflow-scrolling: none !important;
    /* Prevent any resizing */
    touch-action: pan-y !important;
    /* Force hardware acceleration */
    -webkit-transform: translateZ(0);
    transform: translateZ(0);
  }

  /* Make the root element scrollable instead */
  #root, .app-container {
    position: absolute !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    bottom: 0 !important;
    width: 100% !important;
    height: 100% !important;
    overflow-y: auto !important;
    overflow-x: hidden !important;
    -webkit-overflow-scrolling: touch !important;
    /* Fill the entire safe area, including notches and home indicator */
    padding: env(safe-area-inset-top, 0px) 
             env(safe-area-inset-right, 0px) 
             env(safe-area-inset-bottom, 0px) 
             env(safe-area-inset-left, 0px) !important;
    /* Force hardware acceleration */
    -webkit-transform: translateZ(0);
    transform: translateZ(0);
    /* Ensure it fills viewport */
    min-height: -webkit-fill-available !important;
    min-height: 100vh !important;
    min-height: 100svh !important;
    /* Force it to extend all the way to the bottom */
    margin-bottom: 0 !important;
    /* Prevent any potential extra whitespace */
    box-sizing: border-box !important;
  }

  /* Bottom Navigation Bar Fix */
  .bottom-nav {
    position: fixed;
    bottom: env(safe-area-inset-bottom);
    left: 0;
    right: 0;
    width: 100%;
    padding-bottom: env(safe-area-inset-bottom);
    background-color: #111827; /* Adjust to your theme color */
    z-index: 10; /* Ensure it stays on top */
  }

  /* Adjust main content height to avoid nav bar overlap */
  .main-content {
    padding-bottom: calc(60px + env(safe-area-inset-bottom)); /* Adjust 60px based on your nav height */
  }
}